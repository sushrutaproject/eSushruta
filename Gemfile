source "https://rubygems.org"

# Matches what GitHub Pages itself builds with, so `bundle exec jekyll serve`
# locally behaves the same as the deployed site.
gem "github-pages", group: :jekyll_plugins
